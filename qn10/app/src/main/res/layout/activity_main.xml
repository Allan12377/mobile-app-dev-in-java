<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="20dp"
    android:gravity="center_horizontal">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="BodaBoda Location Tracker"
        android:textSize="24sp"
        android:textStyle="bold"
        android:layout_marginBottom="20dp" />

    <!-- Button to request current GPS coordinates -->
    <Button
        android:id="@+id/btnGetLocation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Get My Location" />

    <!-- TextView to display Latitude -->
    <TextView
        android:id="@+id/tvLatitude"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Latitude: --"
        android:textSize="18sp"
        android:layout_marginTop="15dp" />

    <!-- TextView to display Longitude -->
    <TextView
        android:id="@+id/tvLongitude"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Longitude: --"
        android:textSize="18sp"
        android:layout_marginTop="5dp" />

    <!-- Button to share location details via Intent -->
    <Button
        android:id="@+id/btnShareLocation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Share Location"
        android:layout_marginTop="20dp" />

    <!-- Label with map link information -->
    <TextView
        android:id="@+id/tvMapLink"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Tap 'Share' to send coordinates to dispatch."
        android:textColor="#555"
        android:layout_marginTop="10dp"
        android:gravity="center" />

</LinearLayout>